{
  "name": "session-shelf",
  "version": "0.1.0",
  "description": "Local cross-platform desktop manager for Codex JSONL sessions",
  "license": "MIT",
  "private": true,
  "type": "module",
  "workspaces": ["frontend"],
  "scripts": {
    "build": "npm run build --workspace frontend",
    "build:frontend": "npm run build --workspace frontend",
    "check": "node --check server.mjs",
    "dev": "node server.mjs",
    "start": "node server.mjs"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "trash": "^10.1.1",
    "vite": "^7.3.6"
  }
}
